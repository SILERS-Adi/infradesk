psutil>=5.9.0
requests>=2.31.0
websocket-client>=1.9.0
pystray>=0.19.5
Pillow>=10.0.0
pywebview>=4.0
cryptography>=41.0
pywin32>=306

# Optional — Google Drive backup uploads
google-api-python-client>=2.0
google-auth>=2.0

# Optional — Sentry error tracking (init only when SENTRY_DSN_AGENT env set)
sentry-sdk>=2.0
